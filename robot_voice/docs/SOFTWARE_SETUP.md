# Software Setup

```bash
cd /home/phuong/robot_voice
bash scripts/install_deps.sh
bash scripts/pi_process.sh check
```

Use `configs/.env.example` or `.env.example` as the environment template.

Pi-only Python packages are listed in equirements-pi.txt.

